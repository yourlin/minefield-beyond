/**
 * Unique identifier for a cell on the game board.
 *
 * Each cell in any topology is assigned a unique numeric ID.
 * The ID is topology-agnostic — it simply distinguishes one cell from another.
 */
export type CellId = number;
